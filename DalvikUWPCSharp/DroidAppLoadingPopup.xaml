<UserControl
    x:Class="DalvikUWPCSharp.DroidAppLoadingPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DalvikUWPCSharp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource SystemControlForegroundChromeMediumBrush}">
        <Grid  Width="400" Height="300" HorizontalAlignment="Center">
            <TextBlock x:Name="textBlock" HorizontalAlignment="Center" Margin="0,100,0,0" TextWrapping="Wrap" Text="Setting up app environment" VerticalAlignment="Top" FontSize="18.667"/>
            <TextBlock x:Name="textBlock2" HorizontalAlignment="Center" Margin="0,135,0,0" TextWrapping="Wrap" Text="Please wait" VerticalAlignment="Top"/>
            <ProgressRing HorizontalAlignment="Center" Margin="0,0,0,50" VerticalAlignment="Bottom" Width="50" Height="50" IsActive="True"/>
        </Grid>
    </Grid>
</UserControl>
